parameters:
    git_dir: .
    bin_dir: vendor/bin
    tasks:
        phpcs:
            standard: ./phpcs.xml
        phpstan:
            autoload_file: ./vendor/autoload.php
            configuration: ./phpstan.neon
            level: 7
